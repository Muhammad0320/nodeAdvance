module.exports = {
  googleClientID: process.env.GOOGLE_CLIENT_ID,
  googleClientSecret: process.env.GOOGLE_CLIENT_SECRET,
  mongoURI: process.env.MONGO_URI,
  cookieKey: process.env.COOKIE_KEY,
};

// Mwtm6G21i22c3k8y

// muhammawwal@005

// sudo apt-get update sudo apt-get upgrade sudo apt-get install redis-server redis-server --version  redis-server  another window redis-cli ping lolwut set foo 42 get foo back to firset window sudo service redis-server start

// localhost port: 6379 // sparkette

//   Wifi from sule
